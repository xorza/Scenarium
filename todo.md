simulate textureformat change
data type on boundary node ports
how cahced workd for subgraphs

Open question (Q1): is rename-undo-after-compaction (C2) an accepted rough edge, or should reconcile invalidate rename entries whose index
it moved? There's no stable per-port identity today, so a clean fix isn't trivial.
