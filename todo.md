simulate textureformat change
data type on boundary node ports
how cahced workd for subgraphs

2. trim_to_limit memmoves the whole buffer prefix + renormalizes ranges on each over-cap push — documented as cheap at ~100 entries;
   revisit only if max_steps grows large. - ringbuffer?
