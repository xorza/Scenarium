[package]
name = "graph"
version = "0.1.1"
edition = "2021"
description = "A graph library for Nodeshop"

[lib]
name = "graph"
crate-type = ["lib"]

[features]
default = []
opencl = ["ocl"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_yaml = "0"
mlua = { version = "0", features = ["lua54", "vendored"] }
ocl = { version = "0", optional = true }
uuid = { version = "1", features = ["v4", "macro-diagnostics", "serde"] }
anyhow = "1"
