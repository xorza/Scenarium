scrolview on newnode popup

review interned string impl
darkroom string interning

cleanup binary caches

file name as argument clap

try repair & broken document

autoupdate image review and autorun node

features = ["builtin-fs-watch", "builtin-random"] - move from scenarium

[dev-dependencies]
scenarium = { workspace = true, features = ["internals"] }

diskcached pinned outputs arent shown

stacking frames (outer, inner, reference)

auto chosen reference  and manually specified

visual node groups

audit test_support and test exposed proxies
move benches to the src to avoid overexposing api

audit allocations overshoot , see where reserve_exxact would be better

pinned output makes node store outputs in ram

callback when eventloop stopped for gui

invert removed retain logic to optimize
