**/.DS_Store
**/._.DS_Store
**/.directory

**/.idea/
**/target/
**/bench-results/

# Workspace members must not carry their own Cargo.lock — the workspace-root
# lockfile is authoritative. Stray package-level locks confuse scanners
# (e.g. Dependabot) into reporting against stale resolutions.
prism/Cargo.lock

/.claude/settings.local.json

/test_output/
/log/

/patch.diff
/style.toml
/config.toml

# Claude Code sandbox sentinel files
/.bash_profile
/.bashrc
/.gitconfig
/.gitmodules
/.profile
/.ripgreprc
/.zprofile
/.zshrc
/.mcp.json
/.idea
/.vscode
/.claude/agents
/.claude/commands
/HEAD
/config
/hooks
/objects
/refs


**/*.log
