<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="csso.Nodeshop.Editor.MainPage"
             Style="{StaticResource ContentPageDefaultStyle}"
             BackgroundColor="{StaticResource ContentBackground}">

    <ContentPage.Resources>
        <ResourceDictionary />
    </ContentPage.Resources>

    <Grid VerticalOptions="Fill"
          HorizontalOptions="Fill">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <!-- <Grid.ColumnDefinitions> -->
        <!--     <ColumnDefinition Width="*" /> -->
        <!-- </Grid.ColumnDefinitions> -->

        <StackLayout Orientation="Horizontal">
            <Button Text="Load" Clicked="Button_OnClicked" />
        </StackLayout>

        <AbsoluteLayout x:Name="NodeAbsoluteLayout"
                        Grid.Row="1"
                        HorizontalOptions="Fill"
                        VerticalOptions="Fill" />
    </Grid>
</ContentPage>